"""Authentication, authorisation, webhook verification and rate limiting.

Enforced in the backend. The frontend hides buttons a role cannot use; that is a courtesy
to the operator, never a control -- every check that matters happens here.
"""
