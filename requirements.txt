pydantic>=2.6
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
xgboost>=2.0
fastapi>=0.110
uvicorn[standard]>=0.27
langgraph>=0.2
langchain-core>=0.3
anthropic>=0.40
httpx>=0.27
pyyaml>=6.0
pytest>=8.0
# Reads the UCI credit-default .xls in the real-data track. Pandas needs it explicitly
# for the legacy .xls format; without it two tests error rather than skip.
xlrd>=2.0.1
# Only needed when RECOVERAI_DB_URL points at Postgres; SQLite needs nothing.
psycopg[binary]>=3.1
